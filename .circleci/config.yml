version: 1.1

jobs:
  build:
    docker:
      - image: cimg/node:16.14
    steps:
      - checkout
      - run: yarn install
  test:
    docker:
      - image: cimg/node:16.14
    steps:
      - checkout
      - run: yarn run test  

workflows:
  version: 1.1
  build_test:
    jobs:
      - build
      - test
